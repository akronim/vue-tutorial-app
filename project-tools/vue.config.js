module.exports = {
  runtimeCompiler: true,
};

// you can change the default configuration
// by adding a file named .vue.config.js

// baseUrl
// This option is used to specify a URL prefix that is used to reach
// the application when it is deployed as part of a larger site.
// The default URL is /.

// outputDir
// This option is used to specify the directory used for building the production
// version of the application. The default directory is dist.

// devServer
// This option is used to configure the development HTTP server.

// runtimeCompiler
// This option enables the runtime compiler, which allows components to define
// their templates using the template property but increases the amount of code
// sent to the browser.

// chainWebpack
// This option is used to configure webpack.
